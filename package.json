{
  "name": "fakergem-showcase",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "foreman": "^2.0.0",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "nf start",
    "start:react": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "7.7.3"
  }
}
